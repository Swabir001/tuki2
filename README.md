# tuki2

This repo is now a runnable React + Vite app.

## Run

```bash
npm install
npm run dev
```

Then open the URL Vite prints (usually `http://localhost:5173`).

## Notes

- The original React/JSX source that was in `test.html` now lives in `src/App.jsx`.

